<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-build-only override: allows plain HTTP to local development
  addresses (Android emulator host loopback, LAN) so the backend can be
  tested without TLS during development. Release builds use the default
  config (HTTPS only, see main AndroidManifest.xml).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
